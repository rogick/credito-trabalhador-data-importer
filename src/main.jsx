import React from 'react';
import { createRoot } from 'react-dom/client';
import EsocialConsignadoApp from './EsocialConsignadoApp.jsx';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EsocialConsignadoApp />
  </React.StrictMode>
);
